# Python-PassWord-Generator

This is a python tool which can used for generating passwords for your social media accounts. It also stores your generated passwords in a text file. 
You can make that file password protected.
Check it out Yourself.

Pre-Requisites to use this :

import random 
import string
